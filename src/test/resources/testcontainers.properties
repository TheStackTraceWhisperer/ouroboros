# Disable the Ryuk container.
# This is often necessary in CI environments where low-level Docker operations
# can be restricted, causing the Ryuk container itself to fail.
# In an ephemeral CI runner, automatic resource cleanup is handled by the
# environment itself when the runner is destroyed.
testcontainers.ryuk.disabled=true